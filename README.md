# PoliteraX Telegram Auth Bot

Telegram-бот аутентификации для сервера **PoliteraX** с поведением, похожим на пример:
- ✅ «Авторизация подтверждена…» + кнопка «👤 Выбрать профиль»
- ❌ «Код недействителен или уже использован»

## Как работает
1. Сайт открывает бота с `/start auth_<method>_<nickname>_<telegram>`.
2. Бот валидирует payload.
3. Бот помечает payload как использованный (одноразовый код через SQLite).
4. При первом использовании отправляет сообщение об успешной авторизации и кнопку перехода на выбор профиля.
5. При повторном/невалидном использовании отправляет сообщение об ошибке кода.

## Настройка
1. Установи зависимости:
   ```bash
   pip install -r requirements.txt
   ```
2. Подготовь `.env`:
   ```bash
   cp .env.example .env
   ```
3. Заполни переменные:
   - `TELEGRAM_BOT_TOKEN` — токен бота `@politerax_auth_bot`.
   - `ADMIN_CHAT_ID` — id админского чата (опционально).
   - `PROFILE_SELECT_URL` — куда ведет кнопка «Выбрать профиль».
   - `AUTH_DB_PATH` — путь до SQLite БД для одноразовых кодов.

## Запуск
```bash
python3 bot.py
```

## Файлы
- `bot.py` — логика Telegram-бота и одноразовых кодов.
- `auth_payload.py` — парсер payload `/start`.
- `.env.example` — пример конфигурации.
- `requirements.txt` — зависимости Python.
